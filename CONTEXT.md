# TapCanvas 智能体工作台

本上下文定义 TapCanvas 将原生 Harness 收编为内置智能体工作台时的核心业务术语，用于统一会话、工具与身份归属的表达。

## 语言

**智能体运行时**：`apps/agents` 中唯一负责会话执行、模型流、工具调用与子代理的原生 Harness 运行时。
_避免_：小T 后端、Agents Bridge、第二 Agent。

**工作区会话**：绑定一个用户、项目和画布的 Harness 原生会话；其完整记录由 JSONL 保存，TapCanvas 只保存索引与作用域。
_避免_：前端聊天缓存、双份会话历史。

**工具宿主**：在智能体运行时内注册和执行 `tapcanvas_*` 工具的职责；工具宿主依据工作区会话的作用域向 TapCanvas 业务后端请求已授权动作。
_避免_：Hono Agent 编排器、浏览器工具执行器。

**统一浏览器入口**：由 `apps/agents` 原生 Web Server 提供的唯一浏览器 Origin，同时承载 TapCanvas 画布构建产物、Harness 浏览器 RPC 和同源业务转发；开发与生产均不把 Vite Dev Server 作为浏览器入口。
_避免_：5175 与 3080 跨 Origin 直连、独立 Harness 页面。

**业务后端**：负责项目、画布、资产、生成和授权等确定性业务动作的 TapCanvas 后端；它不决定智能体的语义计划或工具选择。
_避免_：小T 决策中枢、Agent Runtime。

**旧链路**：`apps/agents-cli`、Agents Bridge 与 `/public/chat` 构成的既有小T执行链；迁移期保留源码和手动启动能力，但不处理新的工作区会话。
_避免_：默认 Agent 链路、并行会话运行时。

**执行策略**：由前端在对话入口显式生成、仅绑定当前画布项目的本轮 Agent 执行授权；未选择时为 `autonomous`，允许 Agent 在该项目内自主执行包括删除、覆盖、批量修改和计费生成在内的工具动作。
_避免_：全局权限开关、跨项目授权、从聊天文本推断权限。

**执行事实**：运行时为回合恢复、交付验收和故障定位保留的最小 trace/diagnostics，不构成独立的业务审计系统，也不提供审计报表。
_避免_：业务审计日志、审计页面、第二套操作记录。

**内部源码基线**：迁入 `apps/agents` 后由 TapCanvas 维护的完整 Harness 源码副本；它是唯一构建和运行来源，不通过运行时或构建脚本自动同步外部来源。
_避免_：第三方运行副本、自动上游同步、平行 Harness。

**迁移验收**：对统一入口、身份、会话恢复、工具联动、并发写入和旧链路隔离逐项完成的切换门禁；门禁通过前不删除外部来源，门禁通过后才允许移除来源目录。
_避免_：双运行时对比、静默回退、未验收删除。
